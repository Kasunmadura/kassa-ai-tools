# secrets.env.example
# Template only — NOT the real file, safe to keep in this OneDrive-synced folder.
#
# The REAL secrets.env (with your actual token) must live OUTSIDE OneDrive at:
#   C:\Users\<you>\.jira-daily\secrets.env
# Create it by running: .\Init-LocalSecrets.ps1  (in this same prep folder)
#
# Format reference (do not fill in real values here):
JIRA_PAT=
JIRA_BASE_URL=https://tower.catchsoftware.net/jira
